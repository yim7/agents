# AGENTS.md

## 基本约定

- 使用中文交流和编写文档。
- 提出改进建议或动手修改前，先核对相关代码和说明，了解已有能力、处理方式及范围约定，再决定是否需要调整。不要把未经核实的猜测当作项目缺口，也不要重复实现已有能力。
- 优先采用能满足当前需求的简单方案，避免过早分层和抽象。

## 协作与交付

- 用户的追问、质疑、比较和未来设想，通常是为了理解和评估，不代表要求增加功能或进入下一阶段。用户明确要求修改或执行时，按要求继续，不重复确认已有授权。
- 发现新问题时，先判断它是否影响当前功能正常使用或通过验收。必要的修复和验证自行完成，其他改进留待后续；如果完成当前任务确实需要明显增加工作量或复杂度，提出需要用户决定的取舍，并说明原因、代价和建议。
- 需要用户决策时，先完成当前阶段内可独立推进的工作，再集中提出必要问题并附建议；只有用户明确要求逐题访谈时，才逐题等待回答。
- 先把约定的可用结果做完整，让用户实际使用后再迭代。讨论开始偏离当前目标时，主动提醒并建议先完成这一版；达到约定的完成条件就交付，不因为还有改进空间而不断追加工作。

## 工作流

- 开发交付：使用 `deliver` 技能。
- Git 提交：使用 `git-commit` 技能。
- 项目配置：使用 `project-conventions` 技能。

## 安全原则

- 不提交密钥、访问令牌或会话记录。
- 在隔离、限额、个人使用的场景中，优先采用简单、可验证、可恢复的方案；安全措施应针对实际风险，不因假设性担忧增加强制步骤。
- 资金执行功能正式投入前，必须完成可验证的端到端测试；局部步骤成功不代表整体流程可用。
