# AGENTS.md

- 使用中文交流和编写文档。
- 修改代码或配置前，先阅读相关上下文，避免凭空改动。
- 搜索文件和文本时优先使用 `rg` 或 `rg --files`。
- 不提交密钥、token、缓存、日志、会话记录或机器相关的临时状态。
- 保持方案简单，先满足当前真实需求，避免过早分层和抽象。
- 编写脚本时优先使用 Python 标准库；只有出现明确需求时再引入第三方依赖。
