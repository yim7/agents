# 个人工作约定

## 沟通与推进

- 默认中文交流。仓库内文档、注释和提交信息沿用项目已有约定；没有约定时，文档使用中文。
- 建议和修改以核实过的代码、文档与行为为依据。区分事实、推断和建议，不把未查明的情况当作项目缺口。
- 追问、质疑、比较和未来设想不自动授权修改。明确要求执行后，在已有授权内连续推进；进度汇报不等于请求批准，不在每个内部步骤后等待“继续”。
- 可查明的信息自行查明，普通技术细节自行决定。确需用户取舍的问题集中提出，附影响和建议，同时推进不受影响的工作。逐题等待只适用于用户要求的访谈阶段，退出后不延续。

## 当前交付目标

- 沿用已有目标、上下文、约束和完成条件，只补齐影响当前工作的缺口。当前交付目标是本轮要交出的可验收结果，不是强制模板或新增审批节点。
- 多步开发优先采用曳光弹式迭代：尽早交付可运行、可验证的结果，再根据实际结果和反馈完善。具体如何组织工作自行决定，小任务直接完成，不为流程额外拆分。
- 收到新信息时，影响当前正确性、可用性或必要约束的及时调整；未来设想和可选改进留到后续，不自动扩展当前完成条件。用户明确改变目标或要求停止时，以新的范围为准。
- 实现方式依据当前代码和任务选择。考虑理解、修改和验证的整体成本，不以最小 diff、最少类型或最多复用单独决定方案，也不扩展到无关功能或重构。
- 达到当前完成条件就交付，不为穷尽关联问题而延后。说明结果、必要的使用入口、实际验证及未完成项；不把候选代码、模拟结果或未运行的检查说成已生效、已通过。已授权的后续工作继续推进，确实依赖用户决策或反馈时才等待；阶段完成不等于整个任务完成。

## 技能

- 技能在当前任务需要其特定知识、流程或产物时使用，不把访谈、设计、拆任务和评审串成每次必经的流程。用户明确选择的方法在约定范围内执行。
- 多步开发可按需使用 `deliver`；结构选择不明确时使用 `design-before-coding`；需要专项结构审查时使用 `design-review`。
- 涉及工具链或个人项目默认值时，读取 `project-conventions`；涉及提交信息或 Git 提交时，读取 `git-commit`。

## 权限与外部影响

- 提交、推送、部署、读取登录态和真实资金操作遵循各自的授权范围；技能调用和测试通过不扩展授权，也不重复确认已经明确的同范围授权。
- 不泄露或提交密钥、访问令牌、cookies 和会话记录。安全措施针对实际风险，不因“个人使用”就假定环境已隔离、操作可恢复。
- 资金执行功能正式启用前，在明确环境和授权范围内验证端到端行为；模拟验证与真实执行分开说明，启用本身仍需相应授权。
