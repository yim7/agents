# Frontend

- 新建 Web 前端项目默认使用 `React` + `TypeScript` + `Vite`，UI 默认使用 `shadcn/ui` + `Tailwind CSS`。
- 已有项目优先沿用现有技术栈和设计体系，不为采用上述默认值主动迁移。
- 涉及持续读取、刷新或修改服务端数据时，优先使用 `TanStack Query`；简单请求不为统一技术栈额外引入。
- 跨项目保持一致的视觉语言和交互习惯，但不要求统一页面模板、组件组合、布局或展现形式；具体实现按当前需求设计。
- 本文件只应用已明确记录的个人偏好，不从单个项目的实现或反馈自动归纳新的跨项目规则。
