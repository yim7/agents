---
name: software-design
description: 为当前软件问题形成或调整设计方案，明确必要的职责、接口、状态归属和取舍。用于用户要求设计、比较方案，或当前实现存在需要明确的结构选择；普通实现细节无需单独启动，不用于视觉设计。
---

# 软件设计

主要产物是推荐方案、取舍依据和必要边界，不限定在编码前使用。已有决定仍适用时直接沿用；发现反证时说明需要调整的依据。

## 决策依据

核对相关代码、调用方和已确认记录。把项目硬约束、当前实现事实、默认偏好和新的建议区分开；不能因为现状如此就认定它不可改变，也不能把设想的理想架构当成既有约束。

保留现有结构、拆分职责、引入类型、合并逻辑或局部重复，都是候选手段。根据当前职责、变化关系、依赖和验证成本判断，不预设某种代码形态更优。不为尚无依据的未来需求设计通用机制，也不为减少本次 diff 延续已经暴露的问题。

存在真实取舍时比较合理方案，并考虑改变现状的成本；不凑固定数量的备选方案。最终结构与迁移路径分别判断，允许有必要且有退出条件的过渡方案。

## 交付与边界

用与问题规模相称的篇幅给出：解决哪个选择、推荐什么方案、依据哪些约束、主要代价，以及实现或验证所需的边界。只覆盖与当前选择有关的内容，不以一份问题清单代替方案。

需要跨任务保存的决定写回现有 Spec、任务或设计记录；普通可逆的实现细节不另建文档，不复制已有规格。不确定事项保持为不确定，不冒充用户已确认的决定。

只要求设计时，交付方案，不直接修改实现；已有实现授权时，结论足以支持当前工作后继续原任务，不扩大范围或新增审批步骤。

设计过程中应检查方案风险，但不默认启动 `design-review`。用户同时要求审查与修订时，在已有授权内交付相应结果，不因技能切换重复请求确认。
